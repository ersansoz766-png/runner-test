name: external-data-injection-fixed
on: 
  issues:
    types: [opened]
jobs:
  process:
    runs-on: [self-hosted, Windows, X64]
    outputs:
      issue_title: ${{ github.event.issue.title }}
    steps:
      - name: Receive Issue
        shell: cmd
        run: echo Issue received
      
  execute:
    needs: process
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Process Issue Title
        shell: cmd
        run: |
          echo Processing issue title:
          set TITLE=${{ needs.process.outputs.issue_title }}
          echo Title: %TITLE%
